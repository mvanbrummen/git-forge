http {
  interface = "0.0.0.0"
  port = 8080
}

jwt {
  secret = "secret-squirrel"
  secret = ${?JWT_SECRET}
}

db {
  url = "jdbc:postgresql://localhost:5432/gitforge"
  url = ${?JDBC_URL}
  user = "myuser"
  user = ${?JDBC_USER}
  password = "sshh"
  password = ${?JDBC_PASSWORD}
}

